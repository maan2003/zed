- use jj workspaces
- file selection without using current project
- showing diffs generated by agent
- running `cargo c` against the workspace
- checkout workspace
- fast edit by just looking at things and typing - kind of pseudo vim mode
- quick review feedback of what you are looking at without needing to select stuff
- muliple agents should work concurrently (in different working workspaces)
- todo manager for ai
  - happen before relationship between things
  - and parallelize as much as possible
- ai to execute cli commands (maybe use tasks)
- add http api to chat on phone
  - is this that important to spend more time
  - can we use vnc to just view zed on phone
- floating small window when moving away from editor

zed team will likely get to:
- show thinking in ui
- persist message history
- side by side diff
- programs ran as context? (maybe just terminal is enough)

WORKSPACES:
- each agent should use jj workspace
- muliple agents should work concurrently
- run shell commands
- ability to copy history to new agent workspace.
- state of jj workspace: active(workspace), inactive(deleted)
- merging of agent's workspace into main workspace

why use zed:
+ markdown chat rendering
+ it is code editor
+ good llm apis
- have to sync with upstream

why use http server + web ui
+ multiple client: can view on phone
- less experience
- separate server to manage and have to define a protocol and sync state
- have to move to editor to edit stuff

- we are going to use pane for assistant view to have multiple active threads

current state:

- moved assistant2 panel to use panes instead
- had to nuke bunch of assistant2: configuration and history view
